{
  "name": "pupjs",
  "version": "1.0.0",
  "description": "Extracts JavaScript and JSON resource URLs from a webpage using Puppeteer.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^1.9.0"
  }
}
